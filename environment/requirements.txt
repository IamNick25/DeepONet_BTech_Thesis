# ---------------------------------------------------------------------------
# Python dependencies for the DeepONet / U-DeepONet PCM surrogate notebooks.
#
# Only packages that are actually imported somewhere in this repository are
# listed. Versions are pinned ONLY where the original notebook outputs record
# the version that was used; everything else is left unpinned deliberately
# rather than invented.
#
# Recorded from the archived notebook outputs:
#   torch 2.5.1+cu121      (the 2-D / 3-D U-DeepONet training runs)
#   Jupyter kernels        Python 3.12.0, 3.12.7 and 3.14.3
#   All archived training logs report DEVICE: cpu, i.e. the recorded runs were
#   executed on CPU. A CUDA build of torch is optional; the notebooks select
#   the device with torch.cuda.is_available().
# ---------------------------------------------------------------------------

numpy
scipy
pandas
matplotlib
scikit-learn
scikit-image        # skimage.measure - marching cubes for the 3-D iso-surface plots
joblib              # Parallel/delayed in the 3-D dataset generator
torch==2.5.1        # install the +cu121 wheel from https://pytorch.org if a GPU is used
jupyterlab
ipykernel
